build/
managed_components/
sdkconfig
sdkconfig.old
dependencies.lock
.vscode/
*.pyc
__pycache__/

# Sweep logs — noisy, reproducible from raw data
visualizer/sweep_*.log
visualizer/retry_*.log
visualizer/resume_*.log

# Python venv used by measure.py / visualizer / analyze.py
visualizer/venv/

# WiFi credentials — never commit
main/wifi_credentials.h

# transient run logs
*.log
viz.pid

# firecrawl scrape cache (transient, may contain tracking URLs)
.firecrawl/

# Obsidian workspace state
.obsidian/

# CAD binaries — hundreds of MB, versioned locally + backed up by
# cad/backup_solidworks.ps1. The MEASURED geometry (extrinsics_measured.json)
# and all cad/*.md docs ARE committed.
*.SLDPRT
*.SLDASM
*.SLDDRW
*.sldprt
*.sldasm
*.step
*.STEP
*.stl

# camera: raw calibration video (regenerable; extracted shots + npz are committed)
camera/calib_video.avi
camera/calib_video.mkv

# YOLO weights — ultralytics re-downloads them by name
*.pt
camera/vlm_log.jsonl
camera/nim_key.txt
yolov8s-worldv2.pt
camera/voice_log.jsonl
camera/models/
camera/sessions/
camera/stride_cal.json
camera/gate_log.jsonl
camera/ocr_log.jsonl
launcher_icon.png
