/*
 * Template for main/wifi_credentials.h.
 *
 *   1. Copy this file to main/wifi_credentials.h
 *   2. Fill in the values below
 *   3. Update the matching OTA_TOKEN in visualizer/ota_flash.py
 *
 * The real wifi_credentials.h is in .gitignore — it MUST NEVER be committed.
 */
#pragma once

#define WIFI_SSID     "your-wifi-network-name"
#define WIFI_PASSWORD "your-wifi-password"

/* TCP port the data stream listens on (DATA:/SIGMA: lines). */
#define TCP_PORT      3333

/* HTTP port the OTA endpoint listens on. POST /update. */
#define OTA_HTTP_PORT 80

/* Shared secret required in the X-OTA-Token header for OTA pushes.
 * Pick anything reasonably random. Must match OTA_TOKEN in
 * visualizer/ota_flash.py — the host script sends this in its header. */
#define OTA_TOKEN     "change-me-pick-a-random-string"
